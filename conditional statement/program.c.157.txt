#include <stdio.h>

int main()
{
    int n,r,count=0;

    scanf("%d",&n);

    while(n>0)
    {
        r=n%10;

        if(r%2!=0)
            count++;

        n=n/10;
    }

    printf("Odd digits = %d",count);

    return 0;
}