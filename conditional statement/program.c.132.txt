#include <stdio.h>
int main() {
    int num;

    printf("Enter number: ");
    scanf("%d",&num);

    if(num>100)
        printf("Greater than 100");
    else
        printf("Not greater than 100");

    return 0;
}