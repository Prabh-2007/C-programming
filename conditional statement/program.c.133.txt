#include <stdio.h>
#include <math.h>

int main() {
    int num;
    int root;

    printf("Enter number: ");
    scanf("%d",&num);

    root = sqrt(num);

    if(root*root == num)
        printf("Perfect Square");
    else
        printf("Not Perfect Square");

    return 0;
}