#include <stdio.h>

int main() {
    int marks;

    printf("Enter marks: ");
    scanf("%d", &marks);

    if(marks < 0 || marks > 100)
        printf("Invalid Marks");
    else if(marks >= 50)
        printf("Pass");
    else
        printf("Fail");

    return 0;
}