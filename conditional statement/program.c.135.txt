#include <stdio.h>
int main() {
    int num;

    printf("Enter number: ");
    scanf("%d",&num);

    if(num>=50 && num<=100)
        printf("Number in range");
    else
        printf("Number not in range");

    return 0;
}