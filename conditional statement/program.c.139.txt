#include <stdio.h>
int main() {
    int amount;

    printf("Enter purchase amount: ");
    scanf("%d",&amount);

    if(amount>1000)
        printf("Discount Available");
    else
        printf("No Discount");

    return 0;
}