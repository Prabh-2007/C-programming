#include <stdio.h>

int main()
{
    int amount;

    scanf("%d",&amount);

    if(amount > 2000)
        printf("20%% Discount");
    else if(amount > 1000)
        printf("10%% Discount");
    else
        printf("No Discount");

    return 0;
}