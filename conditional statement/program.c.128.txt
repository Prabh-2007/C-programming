#include <stdio.h>
int main() {
    int unit;

    printf("Enter units: ");
    scanf("%d",&unit);

    if(unit<100)
        printf("Low usage");
    else if(unit<=300)
        printf("Medium usage");
    else
        printf("High usage");

    return 0;
}