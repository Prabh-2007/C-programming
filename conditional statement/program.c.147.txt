#include <stdio.h>

int main()
{
    int n,r,flag=0;
    scanf("%d",&n);

    int temp=n;

    while(n>0)
    {
        r=n%10;
        if(r==0)
        flag=1;

        n=n/10;
    }

    if(flag==1)
        printf("Duck Number");
    else
        printf("Not Duck Number");

    return 0;
}